# # 🎨 Gazisiber-Fetch

**Smart, Responsive System Information Display for Gazi Siber Güvenlik**

A beautifully designed fastfetch configuration that automatically adjusts logo and text layout based on your terminal size, ensuring perfect balance and readability on any screen.

---

## ✨ Features

- 🎯 **Auto-Responsive Design** - Logo and text automatically adjust based on terminal dimensions
- 🎨 **Professional Branding** - Custom Gazi Siber logo with color-coded system information
- 🖥️ **Smart Padding** - Dynamic spacing calculations for optimal visual balance
- ⚡ **Fast & Lightweight** - Built on fastfetch for maximum performance
- 🔧 **Easy Installation** - One-command setup with automated configuration

---

## 📦 How It Works

The `fetch.sh` wrapper script intelligently:

1. **Detects** your terminal dimensions (width × height)
2. **Calculates** optimal padding values for logo placement
3. **Adjusts** the configuration dynamically
4. **Displays** perfectly balanced system information

### Responsive Behavior

| Terminal Width | Left Padding | Right Padding | Notes |
|---------------|--------------|---------------|-------|
| ≥ 150 cols    | 4            | 8             | Very wide terminals - generous spacing |
| ≥ 120 cols    | 3            | 6             | Wide terminals - comfortable spacing |
| ≥ 100 cols    | 2            | 5             | Medium-wide terminals |
| ≥ 80 cols     | 2            | 4             | Standard terminals |
| < 80 cols     | 1            | 2             | Narrow terminals - minimal spacing |

### Height Adjustments

- **≥ 40 lines**: Top padding = 2
- **≥ 30 lines**: Top padding = 1  
- **< 30 lines**: Top padding = 0

This ensures the logo and text are always proportional and suitable to each other!

---

## 🚀 Installation

```bash
# Clone the repository
git clone https://github.com/AbdullahZeynel/gazisiber-fetch.git
cd gazisiber-fetch

# Run the installation script
chmod +x install.sh
./install.sh
```

The installer will:
- ✅ Check for fastfetch
- ✅ Copy configuration files to `~/.config/fastfetch/`
- ✅ Install the smart wrapper script
- ✅ Create the `gazisiber-fetch` command globally

---

## 📖 Usage

Simply run:

```bash
gazisiber-fetch
```

Or use the script directly:

```bash
./fetch.sh
```

To test different terminal sizes, resize your terminal and run the command again!

---

## 🎨 Customization

### Modify Logo
Replace `logo.txt` with your own ASCII art logo.

### Adjust Responsive Thresholds
Edit the `calculate_padding()` function in `fetch.sh` to customize breakpoints:

```bash
# Example: Make spacing more generous
if [ $cols -ge 100 ]; then
    left=4
    right=8
fi
```

### Change Colors & Modules
Edit `config.jsonc` to customize:
- Module order and visibility
- Color schemes
- Separators and formatting
- Additional system information

---

## 📂 File Structure

```
gazisiber-fetch/
├── fetch.sh              # Smart wrapper with auto-sizing logic
├── config.jsonc          # Base fastfetch configuration
├── logo.txt              # ASCII logo file
├── logo_ascii.txt        # Alternative logo (larger)
├── install.sh            # Installation script
└── README.md             # This file
```

---

## 🛠️ Requirements

- **fastfetch** >= 1.0.0
- **bash** >= 4.0
- **tput** (usually pre-installed)

### Installing Fastfetch

**Ubuntu/Debian:**
```bash
sudo add-apt-repository ppa:zhangsongcui3336/fastfetch -y
sudo apt update
sudo apt install fastfetch -y
```

**Arch Linux:**
```bash
sudo pacman -S fastfetch
```

**Fedora:**
```bash
sudo dnf install fastfetch
```

---

## 🤝 Contributing

Contributions are welcome! Feel free to:
- Report bugs
- Suggest new features
- Submit pull requests
- Share your custom configs

---

## 📝 License

See [LICENSE](LICENSE) file for details.

---

## 🔗 Connect

- 🌐 **Website**: github.com/gazisiber
- 📷 **Instagram**: instagram.com/gazisiber

**Daha Güvenli Bir Gelecek İçin** 🛡️

---

*Generated by godfry for Gazi Siber Güvenlik Araştırma ve Geliştirme Topluluğu*